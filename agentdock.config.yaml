version: "1"
resources:
  sync: []
hooks:
  afterCreateSession:
    - run: bun install
      required: true
      timeout: 120000
      cwd: worktree
      async: false
env:
  ports:
    - FRONTEND_PORT
    - BACKEND_PORT
    - WS_PORT
    - DEBUG_PORT
    - PREVIEW_PORT
