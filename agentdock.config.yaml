version: "1"
resources:
  sync: []
hooks:
  afterCreateSession:
    - run: "bun i --ignore-scripts"
      required: false
      timeout: 60000
      cwd: worktree
env:
  ports:
    - FRONTEND_PORT
    - BACKEND_PORT
    - WS_PORT
    - DEBUG_PORT
    - PREVIEW_PORT
